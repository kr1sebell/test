[project]
name = "testproject"
version = "0.1.0"
description = "Пример каркаса проекта"
authors = [{name = "Пример автора"}]
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
